<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.linkopener">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LinkOpener"
        tools:targetApi="31">
        <activity
            android:name=".screens.AddUserActivity"
            android:exported="false" />
        <activity
            android:name=".screens.AddPetActivity"
            android:exported="false" />
        <activity
            android:name=".screens.AddTicketActivity"
            android:exported="false" />
        <activity
            android:name=".screens.CheckTicketActivity"
            android:exported="false" />
        <activity
            android:name=".screens.DatabaseActivity"
            android:exported="false" />
        <activity
            android:name=".screens.InputLinkActivity"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden"
            android:exported="false" />
        <activity
            android:name=".screens.MainActivity"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>